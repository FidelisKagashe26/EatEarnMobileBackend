# Copy to .env and adjust as needed. All values have safe dev defaults.
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*
OTP_TTL_MINUTES=10
# Return the OTP code in API responses + console (dev only; turn off in prod)
OTP_DEBUG_RETURN=True
